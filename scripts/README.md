# Scripts

This directory contains helper scripts for development and gameplay.

* `run-todo-server.sh [port]` - Launches a simple HTTP server to preview the todo demo.
  Pass a custom `port` to override the default of `8000`.

