# FAQ

## How do I build Cosmic?
Run `./build.sh` to compile the server using the Maven wrapper.

## Where are the configuration files?
All server configuration lives in `config.yaml`.

## How do I run tests?
Run `./run_tests.sh` to execute the test suite.
