# Helper Scripts

This directory contains small utility scripts used during development.

- `list_todos.sh` - Search the repository for `TODO` comments.
- `run-tests.sh` - Execute the Maven test suite.
- `unzip_here.sh` - Quickly extract a zip archive in-place.
- `list_handbook.sh` - List available files inside the `handbook/` directory.
