name: Lint Markdown
on:
  pull_request:
    paths:
      - '**/*.md'

jobs:
  markdown:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Lint docs
        uses: avto-dev/markdown-lint@v1

